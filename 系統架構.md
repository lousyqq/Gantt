# 系統架構.md — MSD 專案追蹤總表

## 技術棧

| 層 | 技術 |
|----|------|
| 前端 | React 18（UMD 本地化於 `wwwroot/lib/`，無 CDN）＋ Tailwind CSS；原始碼 `ClientApp/app.jsx` 經 Babel CLI 預編譯為 `wwwroot/app.js` |
| 後端 | ASP.NET Core 9 Minimal API（根目錄 `Gantt.csproj`／`Program.cs`），ADO.NET（Microsoft.Data.SqlClient）直呼預存程序 |
| 資料庫 | SQL Server 2019（開發 `Sariel\Gantt`；遠端正式主機四個 DB：`Gantt`／`Gantt_IMD`／`Gantt_EMS1`／`Gantt_EMS2`，結構相同）；結構詳見 `DB_table.md` |
| 驗證 | Negotiate（Windows 驗證）僅用於 `/api/whoami` 取工號；其餘端點匿名 |
| 匯出 | ClosedXML 0.105.0（週報／成果清單 .xlsx） |
| 部署 | **同一份程式發佈到四個 IIS application**（`/Gantt`＝MSD、`/Gantt_IMD`、`/Gantt_EMS1`、`/Gantt_EMS2`），後端以 `Request.PathBase` 當 key 查 `appsettings` 的 `Sites` 段落取群組名稱與連線字串（2026-09-13）；需啟用 Windows 驗證＋匿名驗證並存 |

## 模組圖與資料流

```
瀏覽器 SPA (app.js, 單一 App 元件)
  │  載入時: GET /api/whoami (Negotiate→工號) → GET /api/access-check (瀏覽權限, fail-closed)
  │          → GET /api/bootstrap?year= (一次載入全部資料) → 60 秒靜默輪詢 refreshData
  │  寫入:   POST /api/*  (apiPost 統一自動附 actorEmpId)
  ▼
Minimal API (Program.cs)
  │  讀取: bootstrap / audit-log(白話翻譯在此層) / login-stats / access-check(查 notes_person)
  │  寫入: 一律 CommandType.StoredProcedure；錯誤統一 Fail(ex)——RAISERROR(50000)照原文回400、其餘記log回一般化500
  │  站台: ResolveSite() 以 Request.PathBase(IIS application 路徑)查 Sites:{key} → {GroupName, ConnectionString}
  │        ConnStr() 從它出去、每次即時讀 appsettings (reloadOnChange，改檔即生效)；GET /api/site 回群組名稱(不碰 DB)
  ▼
SQL Server（26 個 SP，全部寫 AuditLog；權限檢查在 SP 內做：主管限定/負責人限定）
  └─ 跨 server VIEW [WEB].[dbo].[notes_person]（部門名冊，瀏覽權限比對用；本機以 sim 腳本模擬）
```

## 前端模組（app.jsx 內單檔元件）

- **三種檢視**（viewMode）：週檢視（打卡操作）／年度總覽（整年一頁唯讀）／成果清單 ResultsView（高階主管唯讀＋★重點關注＋排序篩選＋Excel 匯出）。
- **子區間**（遷移 18；遷移 20 起為回報單位）：計畫區間底下可再切多個可重疊的階段。甘特圖在專案列下方以縮排子列逐條顯示
  （名稱後 `▾ n` 晶片展開／收合；週檢視預設展開、年度總覽預設收合，偏好記 `gantt_prefs`），
  編輯入口在 TaskModal 排程卡下方的「子區間」清單，主管與專案負責人可編。
  **回報單位規則**只有一份 `weekUnits()`：該週有進行中的子區間 → 逐子區間打卡（`WeeklyLogs.SubId`），否則對計畫區間打卡；
  父層舊紀錄優先；區間分數＝子區間平均。bootstrap 分 `taskLogs`（父層）與 `subLogs`（子區間）兩份。
- **成員回報動線**：header「📋 本週回報中心」PendingPanel（進度條＋任務清單＋下週預計必填＋非專案選填＋Completion Loop）；
  補登開啟時另有「🕘 修改 W{..} 回報」retro 面板。
- **主管功能**：TaskModal 排程編輯／評分、ProjectEditModal（含改負責人）、IntervalModal、DeliverableModal（產出＋MP）、
  CommentModal（週報回覆）、ManagerWeekPanel（代成員修任一週）、補登總開關；
  「⚙️ 管理 ▾」下拉（header 右側帳號區）收納低頻入口：MemberPanel（成員管理）／AccessPanel（瀏覽權限）／
  UsageStatsPanel（使用統計）／AuditPanel（異動紀錄）。
- **共用機制**：ConfirmModal、Toast（❌開頭=錯誤、action 鈕如「↩ 復原」）、MetaLine（最後編輯時間/人）、
  ESC 關窗鏈 closeTopModal、鍵盤導航（H 回本週/←→ 平移）、登入持久化 `gantt_login`、檢視偏好 `gantt_prefs`、
  WeeklyReportDashboard（團隊總結：角色感知＋複製週報＋Excel 匯出）、DeadlinePanel（到期清單）、
  DocLink／DocUrlField（三個回報項目共用的文件連結：顯示端是 24×24 SVG 圖示、一律 `<a target="_blank">`
  開新分頁，UNC／本機路徑會先轉成合法 `file:` URL，能否開起來取決於瀏覽器政策；輸入端共用同一個欄位與驗證）。
- **週次規則＝公司週（不是 ISO 8601）**：週日起始、W1＝含 1/1 的那一週、跨年照日期切（12/31 屬舊年度、1/1 起屬新年度 W1；
  等同 Excel `WEEKNUM(d,1)`）。同一套規則寫在三處：前端 `companyWeekOf`（今天是第幾週、`weekRangeLabel` 顯示日期區間）、
  後端 `CompanyWeekOf`（未來週不可寫入的第二道防線）、SP `usp_EnsureScheduleYear`（產生年度週表 `ScheduleWeeks`，
  週所屬月份＝該週週日的月份）。改任一處要三處一起改（遷移 21，2026-09-14）。

## 後端端點分組（Program.cs）

| 分組 | 端點 |
|------|------|
| 載入 | GET /api/whoami、/api/bootstrap、/api/access-check |
| 回報 | POST /api/weekly-log、/api/weekly-log/score、/api/extra-note、/api/weekly-plan、/api/weekly-comment |
| 專案/任務 | POST /api/project、/project/update、/project/delete、/project/restore、/project/reorder、/project/deliverable、/api/task、/task/delete、/task/restore、/api/task-schedule、/api/task/sub、/task/sub/delete、/task/sub/restore |
| 成員 | POST /api/user、/user/update、/user/delete |
| 設定 | POST /api/settings/retro-checkin、/settings/access-control；GET/POST /api/access-rules(+add/delete) |
| 統計/稽核/匯出 | GET /api/audit-log、/api/login-stats、/api/weekly-report-excel；POST /api/login-log、/api/results-excel |

## 建置與部署流程

1. 前端：改 `ClientApp/app.jsx` → `npm run build`（產生 app.js/app.css＋`scripts/stamp-assets.js` 自動蓋 `?v=時間戳` 快取破壞）。
2. 後端：`dotnet build Gantt.csproj`；本地執行 `dotnet run --project Gantt.csproj --urls http://localhost:5099`。
3. 發布：`dotnet publish` → IIS 子應用程式（App Pool＝No Managed Code；Windows 驗證＋匿名驗證皆啟用）。
   前端 `API_BASE` 執行期自動偵測子目錄，本地測好可直接發布。
4. DB 變更：新增編號遷移檔（`DB_table.md` 規則），遠端依編號順序執行。
